# hooks/ — shared hooks

Reusable hooks used across features. **Shared layer**: no imports from features
or app. A hook used by a single feature belongs in that feature's `hooks/`.

Example: `use-disclosure.ts`.
