# assets/ — static files

Shared images, fonts, and other static assets imported by more than one feature.
Feature-specific assets belong in `features/<x>/assets`. Prefer modern image
formats (WebP/AVIF) — see `docs/performance.md`.
